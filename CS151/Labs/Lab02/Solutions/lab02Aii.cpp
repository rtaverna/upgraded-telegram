#include <iostream>

int main()
{
	//These are just one possiblity. Your solution may 
	//be different, but correct as well. The format is
	//the important thing to focus on.
	
	std::cout << "4 * 5 + (8 - 4) = " << (4 * 5 + (8 - 4)) << "\n";	
	std::cout << "(1 + 5) * (9 - 5) = " << ((1 + 5) * (9 - 5)) << "\n";
	return 0;
}
